/**
 * Interactive Tests
 */

import { describe, it, expect, vi, beforeEach } from 'vitest';

describe('Interactive', () => {
  describe('prompt', () => {
    it('should create prompt object', () => {
      expect(true).toBe(true);
    });
  });

  describe('select', () => {
    it('should create select object', () => {
      expect(true).toBe(true);
    });
  });

  describe('confirm', () => {
    it('should create confirm object', () => {
      expect(true).toBe(true);
    });
  });
});
